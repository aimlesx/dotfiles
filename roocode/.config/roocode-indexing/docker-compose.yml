services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: unless-stopped
    ports:
      - "6333:6333" # REST
    volumes:
      - ~/.qdrant/data:/qdrant/storage
volumes:
  qdrant_storage:
    name: roocode-qdrant-data
