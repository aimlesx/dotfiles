#!/usr/bin/bash

# Kill any existing wob instances
pkill -x wob

# Create the FIFO socket
WOB_SOCK="$XDG_RUNTIME_DIR/wob.sock"
rm -f "$WOB_SOCK"
mkfifo "$WOB_SOCK"

# Start wob listening on the socket
tail -f "$WOB_SOCK" | wob

